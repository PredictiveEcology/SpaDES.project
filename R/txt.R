.txtButTheProjPathIsTmpDir <- "but the projectPath is the tempdir"
.txtUpdateProfileIsTRUE <- paste0("updateRprofile is TRUE, ", .txtButTheProjPathIsTmpDir,
                                  "(), which means ",
                                  "the .Rprofile won't be read upon restart. ",
                                  "Change the paths$projectPath to a non-temporary path")

.txtSettingUp <- "setting up"