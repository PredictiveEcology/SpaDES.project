% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{register_scenario_format}
\alias{register_scenario_format}
\title{Register a project-specific path builder / parser.}
\usage{
register_scenario_format(build, parse, withFieldLabel)
}
\arguments{
\item{build}{Function (custom path builder), or \code{NULL} to clear.}

\item{parse}{Function (custom path parser), or \code{NULL} to clear.}

\item{withFieldLabel}{Either:
\itemize{
\item a \emph{character vector} of field names (e.g. \code{c(".rep", ".SSP")})
-- those fields' path segments get prefixed with the field name
itself (\code{.rep5}, \code{.SSP370}); or
\item a \emph{named character vector} mapping field name to display label
(e.g. \code{c(.rep = "rep", .SSP = "_ssp")}) -- those fields' path
segments get prefixed with the \emph{mapped} label (\code{rep5}, \verb{_ssp370}).
Pass \code{character(0)} or \code{NULL} to clear.
}}
}
\value{
Invisibly, the current \verb{(build, parse, withFieldLabel)} triple.
}
\description{
Pass a function (or, for \code{withFieldLabel}, a character vector) to
register it; pass \code{NULL} explicitly to clear that slot; omit the
argument to leave it untouched. Call with no arguments to inspect.
}
\details{
Lookup precedence (highest first): registered slot ->
\code{pathBuild}/\code{pathParse} defined in the global environment -> the
package defaults.

Override signature contract:
\code{build(..., pre = "outputs")} --- receives the scenario as named
\code{...} args (one per field) plus \code{pre}; returns a path string.
\code{parse(path, pre = "outputs")} --- returns a named list of fields.
}
