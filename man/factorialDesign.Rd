% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment-legacy.R
\name{factorialDesign}
\alias{factorialDesign}
\title{Build a factorial experiment design}
\usage{
factorialDesign(sim, params, modules, objects = list(), inputs, replicates = 1)
}
\arguments{
\item{sim}{A \code{simList}, acting as the basis for the experiment.}

\item{params}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but for each parameter, provide a list of
alternative values.}

\item{modules}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but a list of \code{module} names (as strings).}

\item{objects}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but a list of named lists of named objects.}

\item{inputs}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but a list of \code{inputs} data.frames.}

\item{replicates}{The number of replicates to run of the same \code{simList}.}
}
\value{
A \code{data.frame}, one row per run.
}
\description{
Extracts the "all meaningful combinations" factorial-design logic that used to
live inside \code{experiment()}. Given a \code{simList} plus lists of alternative
\code{params} / \code{modules} / \code{inputs} / \code{objects}, it returns one row per run.
Values are stored as \emph{indices} into the supplied alternatives (because an
alternative may itself be a vector and so cannot live in a single data.frame
cell); column names are \code{module.parameter}, plus a \code{modules} index, an
\code{expLevel}, and (when relevant) \code{input}, \code{object} and \code{replicate} columns.
}
\details{
This is the engine behind \code{\link[=experiment]{experiment()}}. It is exported so the same design
can also seed the file-queue \link{experiment_family} (\code{experimentFuture()} etc.):
map each row's indices back to values to build their \code{df}.
}
\seealso{
\code{\link[=experiment]{experiment()}}, \link{experiment_family}
}
