% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux.R
\name{tmuxFindDuplicates}
\alias{tmuxFindDuplicates}
\title{Find duplicate worker panes running the same job}
\usage{
tmuxFindDuplicates(panes = NULL, runPattern = "outputs-")
}
\arguments{
\item{panes}{Optional data.frame as returned by \code{\link[=tmuxListPanes]{tmuxListPanes()}}. If
\code{NULL} (the default) one is fetched internally.}

\item{runPattern}{Optional regex; only panes whose stripped title matches
it are considered.  Default \code{"outputs-"} matches this codebase's usual
runName prefix; pass \code{NULL} to disable the filter.}
}
\value{
data.frame with the same columns as \code{\link[=tmuxListPanes]{tmuxListPanes()}} plus
\code{run_id} (the stripped runName used for grouping) and \code{group} (integer
identifying each duplicate set).  Rows are ordered by \code{group} then
\code{pane_ref}.  Empty data.frame (with these columns) when no duplicates.
}
\description{
Strips the leading \code{"<host?>-<node>-<pid>-"} prefix from each pane title
and groups panes whose remainders are identical.  Intended to surface
cases where the same queue row has been claimed by two workers (e.g. a
stale RUNNING reclaim that was actually live).
}
\details{
The prefix strip matches 1 or 2 non-dash chunks followed by a 6+-digit
PID followed by a dash -- covering both \verb{<host>-<node>-<pid>-<runName>}
and \verb{<node>-<pid>-<runName>} title formats.  Old-style titles lacking
this prefix are kept verbatim; a title is considered a duplicate only if
its stripped form appears on 2+ panes, so two differently-formatted titles
with the same tail still collapse correctly.
}
