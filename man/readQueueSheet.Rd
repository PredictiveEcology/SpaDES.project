% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{readQueueSheet}
\alias{readQueueSheet}
\title{Read a Google Sheet by name from a Drive folder.}
\usage{
readQueueSheet(folder, name, sheet = NULL, col_types = "c")
}
\arguments{
\item{folder}{Folder URL or \code{dribble} of the parent folder.}

\item{name}{Spreadsheet name (exact match) within \code{folder}.}

\item{sheet}{Optional worksheet/tab name within the spreadsheet
(passed to \code{googlesheets4::read_sheet()}).}

\item{col_types}{Column-types spec for \code{read_sheet()} (default \code{"c"},
read everything as character — matches \code{.gs_read_queue} and avoids
googlesheets4's type-guessing surprises on mixed columns).}
}
\value{
A tibble. Pipe through \code{\link[=as_scenario]{as_scenario()}} to get scenario records.
}
\description{
Convenience wrapper around \code{googledrive::drive_ls()} +
\code{googlesheets4::read_sheet()} for the common case where the queue
sheet lives inside a shared Drive folder and you only know its name
(e.g. \code{"longRuns"}).
}
