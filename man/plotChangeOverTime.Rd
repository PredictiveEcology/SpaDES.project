% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/studyAreaPlotting.R
\name{plotChangeOverTime}
\alias{plotChangeOverTime}
\title{Difference between two layers of a time-series, as a leaflet map}
\usage{
plotChangeOverTime(
  x,
  from = NULL,
  to = NULL,
  palette = "differences",
  rev = TRUE,
  layerName = "change",
  legendPosition = "bottomright"
)
}
\arguments{
\item{x}{One of: a multi-layer \code{SpatRaster}, a list of single-layer
\code{SpatRaster}s, a \code{simList} (reads \verb{<name>*.tif} from
\code{SpaDES.core::outputPath(x)}), or a length-1 character directory path.}

\item{from, to}{Names (or, if missing, first/last) of the layers to subtract.
\code{result = x[[to]] - x[[from]]}.}

\item{palette}{Palette name. Default \code{"differences"} -- the dedicated
blue->white->red diverging palette from \code{\link[terra:map.pal]{terra::map.pal()}}, purpose-built
for difference maps. Any other \code{\link[terra:map.pal]{terra::map.pal()}} name (e.g. \code{"viridis"})
is also accepted; if \code{terra::map.pal()} doesn't recognise the name, we
fall back to \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} (e.g. \code{"RdBu"}, \code{"Spectral"}).}

\item{rev}{Reverse the palette? Default \code{TRUE} -- with the \code{"differences"}
palette this gives red = negative, blue = positive. Set \code{FALSE} to flip
(the \code{terra::map.pal("differences")} native orientation: red = positive,
blue = negative).}

\item{layerName}{Short prefix for the per-layer GeoTIFF filename.}

\item{legendPosition}{Where to place the legend on the map.
One of \code{"bottomright"} (default), \code{"bottomleft"}, \code{"topright"}, \code{"topleft"}.}
}
\value{
A \code{leaflet} htmlwidget showing the difference layer with a
symmetric (zero-centred) colour scale. Static-safe.
}
\description{
Subtracts the \code{from} layer from the \code{to} layer and plots the result on a
single-layer leaflet map with a diverging palette centred on zero. The
"change from start to finish" view that pairs with \code{\link[=plotTimeSeriesLeaflet]{plotTimeSeriesLeaflet()}}.
}
\seealso{
\code{\link[=plotTimeSeriesLeaflet]{plotTimeSeriesLeaflet()}} for the time-step viewer that
complements this difference view.
}
