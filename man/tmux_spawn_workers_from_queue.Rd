% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux-queue.R
\name{tmux_spawn_workers_from_queue}
\alias{tmux_spawn_workers_from_queue}
\title{Spawn tmux workers that consume a file-backed queue until done}
\usage{
tmux_spawn_workers_from_queue(
  queue_path,
  global_path,
  n_workers = 4,
  start_cmd = "R",
  delay_after_split = 0.2,
  delay_after_layout = 0.2,
  delay_between_R_start = 0,
  delay_before_source = 60,
  stagger_by = 0,
  set_mouse = TRUE
)
}
\arguments{
\item{queue_path}{character; absolute path to \code{queue.rds}.}

\item{global_path}{character; absolute path to \code{global.R} (or your script).}

\item{n_workers}{integer; number of panes to spawn.}

\item{start_cmd}{character; command to start R in the pane (default \code{"R"}).}

\item{delay_after_split}{numeric; tmux settle after splits (default \code{0.2}).}

\item{delay_after_layout}{numeric; tmux settle after layout (default \code{0.2}).}

\item{delay_between_R_start}{numeric; pause after starting R (default \code{0}).}

\item{delay_before_source}{numeric; pane‑internal initial sleep for panes 2..n (default \code{60}).}

\item{stagger_by}{numeric; extra offset per pane beyond pane 2 (default \code{0}).}

\item{set_mouse}{logical; enable tmux mouse via \verb{set-option -g mouse on} (default \code{TRUE}).}
}
\value{
Invisibly returns the vector of worker pane IDs.
}
\description{
Spawns \code{n_workers} panes that run a \strong{worker loop}:
atomically claim the next \code{PENDING} row, set objects (column names → object names),
\code{source(global_path)}, mark \code{DONE}, repeat until no \code{PENDING} remain.

Delays happen \strong{inside pane R}:
\itemize{
\item Pane 1: no delay.
\item Pane i>1: waits \code{delay_before_source + (i-2)*stagger_by} inside R before the \strong{first} claim,
then continues without extra delays.
}
}
