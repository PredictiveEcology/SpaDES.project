% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{reUntar}
\alias{reUntar}
\title{Extract sim tarballs, optionally remapping a path prefix}
\usage{
reUntar(tarballs, pathRemap = NULL, verbose = FALSE)
}
\arguments{
\item{tarballs}{Character vector of paths to local tarballs.}

\item{pathRemap}{Optional named character vector of length 2,
\code{c(old = "/old/prefix", new = "/new/prefix")}, applied to all
\code{tarballs}. If \code{NULL} (default), files are extracted to their original
absolute paths (\code{tar --absolute-names}).}

\item{verbose}{Logical. Pass \code{-v} to \code{tar}. Default \code{FALSE}.}
}
\value{
A character vector (same length as \code{tarballs}) of absolute paths
to the \code{.rds} simList file inside each archive (after any remap),
suitable for \code{\link[=reLoad]{reLoad()}}.
}
\description{
Inverse of \code{\link[=outTar]{outTar()}}. Extracts one or more \code{.tar.gz} archives produced
by \code{\link[=outTar]{outTar()}} / \code{\link[=outSaveTarUpload]{outSaveTarUpload()}}, which contain absolute paths. If
\code{pathRemap} is supplied, the leading path prefix is rewritten on
extraction (handy when the archive was created on another user's
machine, e.g. paths starting with \verb{/home/emcintir/...}).

Path rewriting uses GNU tar's \code{--transform}. On systems without GNU tar,
supply \code{pathRemap = NULL} and the archive's absolute paths are restored
as-is.
}
\seealso{
\code{\link[=reGet]{reGet()}}, \code{\link[=reLoad]{reLoad()}}, \code{\link[=reGetUntarLoad]{reGetUntarLoad()}}, \code{\link[=outTar]{outTar()}}
}
