% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment-legacy.R
\name{experiment}
\alias{experiment}
\title{Run an experiment using \code{\link[SpaDES.core:spades]{SpaDES.core::spades()}}}
\usage{
experiment(
  sim,
  replicates = 1,
  params,
  modules,
  objects = list(),
  inputs,
  dirPrefix = "simNum",
  substrLength = 3,
  saveExperiment = TRUE,
  experimentFile = "experiment.RData",
  clearSimEnv = FALSE,
  notOlderThan,
  cl,
  ...
)
}
\arguments{
\item{sim}{A \code{simList}, acting as the basis for the experiment.}

\item{replicates}{The number of replicates to run of the same \code{simList}.}

\item{params}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but for each parameter, provide a list of
alternative values.}

\item{modules}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but a list of \code{module} names (as strings).}

\item{objects}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but a list of named lists of named objects.}

\item{inputs}{Like for \code{\link[SpaDES.core:simInit]{SpaDES.core::simInit()}}, but a list of \code{inputs} data.frames.}

\item{dirPrefix}{String vector. This will be concatenated as a prefix on the
directory names.}

\item{substrLength}{Numeric. While making \code{outputPath} for each spades call, this
is the number of characters kept from each factor level.}

\item{saveExperiment}{Logical. Should the resulting experimental design be saved
to a file. Default TRUE.}

\item{experimentFile}{String. Filename if \code{saveExperiment} is TRUE; saved to
\code{outputPath(sim)} in \code{.RData} format.}

\item{clearSimEnv}{Logical. If TRUE, then the \code{envir(sim)} of each simList in the
return is emptied, to reduce RAM load. Default FALSE.}

\item{notOlderThan}{Currently unused (kept for back-compatibility).}

\item{cl}{Deprecated and ignored; control parallelism with \code{future::plan()}.}

\item{...}{Passed to \code{\link[=experiment2]{experiment2()}} and onward to \code{\link[SpaDES.core:spades]{SpaDES.core::spades()}}
(e.g. \code{debug}, \code{.plotInitialTime}, \code{cache}, and \code{events} -- see
\verb{Controlling events} in \code{\link[=experiment2]{experiment2()}}).}
}
\value{
Invisibly, a \code{simLists} object. The experimental design list
(\code{expDesign} + \code{expVals}) is attached as an attribute named \code{"experiment"}
on the object's data environment, i.e. \code{attr(out@.xData, "experiment")},
and is also written to \code{experimentFile}.
}
\description{
A wrapper around \code{\link[=experiment2]{experiment2()}} that builds a fully-factorial set of
\code{simList}s from a single base \code{simList} plus alternative \code{params} / \code{modules}
/ \code{inputs} / \code{objects}, then runs them via \code{\link[=experiment2]{experiment2()}}'s \code{future}
backend. The factorial design is built with \code{\link[=factorialDesign]{factorialDesign()}}.
}
\details{
This function (and the \code{simLists} class it produces) was moved here from the
now-unmaintained \code{SpaDES.experiment} package. Two behavioural notes versus the
historical version: parallelism is now controlled by \code{future::plan()} rather
than a \code{cl} cluster object (the \code{cl} argument is accepted but ignored, with a
message), and the return value is a \code{simLists} object (as from
\code{\link[=experiment2]{experiment2()}}) rather than a plain list. The experimental design table is
still saved to \code{experimentFile} and is attached to the result (see Value).
}
\seealso{
\code{\link[=experiment2]{experiment2()}}, \code{\link[=factorialDesign]{factorialDesign()}}, \code{\link[=as.data.table.simLists]{as.data.table.simLists()}},
\link{experiment_family}
}
\author{
Eliot McIntire
}
