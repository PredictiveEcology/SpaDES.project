% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux.R
\name{localHostLabel}
\alias{localHostLabel}
\title{Short friendly name for the local machine}
\usage{
localHostLabel()
}
\value{
Character(1) short name, or \code{NULL} if none could be determined.
}
\description{
Resolves the cluster-facing short name for this host, trying in order:
\enumerate{
\item \verb{/etc/hosts} lookup by a local IP (shortest alias wins);
\item \verb{~/.ssh/config} \code{Host} entry whose \code{Hostname} is a local IP (CRLF-safe);
\item \code{hostname -s}.
}
}
\details{
Useful for deriving the pane-title host prefix when the cluster knows this
machine by a name different from \code{hostname -s} (e.g. mega, whose raw
hostname is the node id but whose cluster alias is \code{mega} via \verb{/etc/hosts}).
}
