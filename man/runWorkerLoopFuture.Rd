% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/future.R
\name{runWorkerLoopFuture}
\alias{runWorkerLoopFuture}
\title{Worker loop for future/cluster-based remote execution}
\usage{
runWorkerLoopFuture(
  queue_path,
  global_path,
  on_interrupt = c("requeue", "fail"),
  ss_id = NULL,
  email = NULL,
  cache_path = NULL,
  runNameLabel = quote(colnames(q)[1:2]),
  activeRunningPath = NULL,
  dots_path = NULL,
  log_file = NULL
)
}
\arguments{
\item{queue_path}{Path to the local RDS queue file.}

\item{global_path}{Path to the R script sourced for each job.}

\item{on_interrupt}{\code{"requeue"} or \code{"fail"}.}

\item{ss_id}{Google Sheets ID for the shared queue (or \code{NULL} for
file-based queue).}

\item{email}{Gargle OAuth e-mail.}

\item{cache_path}{Gargle OAuth cache directory.}

\item{runNameLabel}{Quoted expression for deriving a run name.}

\item{activeRunningPath}{Directory for \code{Running_*.rds} marker files.}

\item{dots_path}{Path to an RDS file whose contents are loaded into
\code{.GlobalEnv} before sourcing \code{global_path}.}

\item{log_file}{Path to the log file for this worker.  If \code{NULL},
output goes to the current connection.}
}
\value{
Invisibly returns the worker identifier string.
}
\description{
A thin wrapper around \code{\link{runWorkerLoop}} that optionally redirects
console output to a log file before entering the job loop.  Used internally
by \code{\link{experimentFuture}} for remote (cluster) workers.  Local
workers use \code{callr::r_bg()} and do not need this wrapper.
}
