% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{reGetUntarLoad}
\alias{reGetUntarLoad}
\title{Download, untar, and load SpaDES sims from Google Drive}
\usage{
reGetUntarLoad(
  gFiles,
  destDir,
  pathRemap = NULL,
  projectPath = getwd(),
  method = c("loadSimList", "readRDS"),
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{gFiles}{Either a Google Drive \code{dribble} (e.g. the output of
\code{\link[=outList]{outList()}} / \code{\link[=outScenarios]{outScenarios()}}) or a character vector of Drive file IDs
or URLs.}

\item{destDir}{Character scalar. Local directory to write tarballs into.
Created if it does not exist.}

\item{pathRemap}{Optional named character vector of length 2,
\code{c(old = "/old/prefix", new = "/new/prefix")}, applied to all
\code{tarballs}. If \code{NULL} (default), files are extracted to their original
absolute paths (\code{tar --absolute-names}).}

\item{projectPath}{Character scalar. Passed to
\code{\link[SpaDES.core:loadSimList]{SpaDES.core::loadSimList()}} for relative-path resolution. Default
\code{getwd()}.}

\item{method}{One of \code{"loadSimList"} (default) or \code{"readRDS"}.}

\item{overwrite}{Logical. Force re-download even if the local file
exists. Default \code{FALSE}.}

\item{verbose}{Logical. Print elapsed time per download. Default \code{TRUE}.}
}
\value{
A named list of \code{simList} objects, one per row of \code{gFiles},
named by the archive's \code{name} (sans \code{.tar.gz}).
}
\description{
Convenience wrapper around \code{\link[=reGet]{reGet()}}, \code{\link[=reUntar]{reUntar()}}, and \code{\link[=reLoad]{reLoad()}} -- the
inverse of \code{\link[=outSaveTarUpload]{outSaveTarUpload()}}. Operates on a batch: typically called
with the multi-row \code{dribble} returned by \code{\link[=outList]{outList()}} / \code{\link[=outScenarios]{outScenarios()}}.
}
\seealso{
\code{\link[=reGet]{reGet()}}, \code{\link[=reUntar]{reUntar()}}, \code{\link[=reLoad]{reLoad()}}, \code{\link[=outSaveTarUpload]{outSaveTarUpload()}}
}
