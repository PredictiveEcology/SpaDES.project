% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{queueRead}
\alias{queueRead}
\title{Read the driver queue sheet from a Drive folder.}
\usage{
queueRead(folder, name, sheet = NULL, col_types = "c")
}
\arguments{
\item{folder}{Folder URL or \code{dribble} of the parent folder.}

\item{name}{Spreadsheet name (exact match) within \code{folder}.}

\item{sheet}{Optional worksheet/tab name (passed to \code{read_sheet()}).}

\item{col_types}{Column-types spec for \code{read_sheet()} (default \code{"c"}).}
}
\value{
A \code{data.table}. Pipe through \code{\link[=as_scenario]{as_scenario()}} for scenario records.
}
\description{
Convenience wrapper around \code{googledrive::drive_ls()} +
\code{googlesheets4::read_sheet()}. The result is passed through
\code{revertDotNames()} so callers see canonical \code{.ELFind}/\code{.GCM}/...
column names rather than the \code{dotELFind}/\code{dotGCM}/... names Google
Sheets forces. As a side effect, the non-meta column names are cached
as the active scenario field set (see \code{\link[=scenarioFields]{scenarioFields()}}).
}
\seealso{
\code{\link[=queueUploadMissing]{queueUploadMissing()}}, \code{\link[=outList]{outList()}}, \code{\link[=outScenarios]{outScenarios()}}
}
