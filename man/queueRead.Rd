% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{queueRead}
\alias{queueRead}
\title{Read the driver queue (local RDS or Google Sheet).}
\usage{
queueRead(folder, name, sheet = NULL, col_types = "c")
}
\arguments{
\item{folder}{Either a local path to an \code{.rds} queue file (when
\code{name} is missing), or a Drive folder URL / \code{dribble} of the
parent folder containing the queue spreadsheet.}

\item{name}{Spreadsheet name (exact match) within \code{folder}.
Omit for local-RDS reads.}

\item{sheet}{Optional worksheet/tab name (passed to \code{read_sheet()}).
Ignored for local-RDS reads.}

\item{col_types}{Column-types spec for \code{read_sheet()} (default \code{"c"}).
Ignored for local-RDS reads.}
}
\value{
A \code{data.table}. Pipe through \code{\link[=as_scenario]{as_scenario()}} for scenario records.
}
\description{
Two call shapes:
}
\details{
\describe{
\item{Local: \code{queueRead("path/to/queue.rds")}}{When the first
argument is an existing local \code{.rds} file and \code{name} is not
supplied, the queue is loaded via \code{readRDS()}. Useful for the
file-backed queues written by \code{experimentTmux()} /
\code{experimentFuture()} / \code{experimentSBATCH()} when no \code{ss_id} was
supplied.}
\item{Google Sheet: \code{queueRead(folder, name)}}{Convenience wrapper
around \code{googledrive::drive_ls()} + \code{googlesheets4::read_sheet()}.
\code{folder} is the Drive folder URL/id, \code{name} is the spreadsheet
name within it.}
}

Either way the result is passed through \code{revertDotNames()} so callers
see canonical \code{.ELFind}/\code{.GCM}/... column names rather than the
\code{dotELFind}/\code{dotGCM}/... names Google Sheets forces. As a side
effect, the non-meta column names are cached as the active scenario
field set (see \code{\link[=scenarioFields]{scenarioFields()}}).
}
\seealso{
\code{\link[=queueUploadMissing]{queueUploadMissing()}}, \code{\link[=outList]{outList()}}, \code{\link[=outScenarios]{outScenarios()}},
\code{\link[=experimentFuture]{experimentFuture()}}, \code{\link[=experimentTmux]{experimentTmux()}}, \code{\link[=experimentSBATCH]{experimentSBATCH()}}
}
