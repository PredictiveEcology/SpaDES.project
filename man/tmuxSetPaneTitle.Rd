% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux.R
\name{tmuxSetPaneTitle}
\alias{tmuxSetPaneTitle}
\title{Set a tmux pane's title by matching its current title}
\usage{
tmuxSetPaneTitle(oldTitle, newTitle)
}
\arguments{
\item{oldTitle}{Character(1). Exact current title to match.}

\item{newTitle}{Character(1). Replacement title.}
}
\value{
Invisibly, a character vector of the pane IDs that were updated
(e.g. \code{c("\%12", "\%33")}).  Prints a message per update and a warning
when no match is found.
}
\description{
Scans every tmux server on this machine (sockets under
\verb{$TMUX_TMPDIR/tmux-<uid>/}) for panes whose current title exactly matches
\code{oldTitle}, then rewrites each to \code{newTitle}.  Useful for upgrading
old-style worker-pane titles (without \verb{<node>-<pid>} prefix) to the new
convention so that \code{.gs_reclaim_dead_jobs()} can recognise them.
}
