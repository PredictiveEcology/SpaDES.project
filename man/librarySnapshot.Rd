% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/librarySnapshot.R
\name{librarySnapshot}
\alias{librarySnapshot}
\alias{syncProjectLibrary}
\alias{libraryInUse}
\alias{snapshotLibrary}
\alias{releaseLibrarySnapshot}
\alias{sweepLibrarySnapshots}
\title{Keep a shared project library safe under concurrent experiment workers}
\usage{
syncProjectLibrary(
  packages,
  libPath = .libPaths()[1],
  activeRunningPath = getOption("spades.activeRunningPath"),
  requireIdle = TRUE,
  installFun = NULL,
  verbose = getOption("Require.verbose", 1)
)

libraryInUse(activeRunningPath = getOption("spades.activeRunningPath"))

snapshotLibrary(
  libPath = .libPaths()[1],
  where = getOption("spades.activeRunningPath"),
  id = paste0(Sys.info()[["nodename"]], "_", Sys.getpid()),
  setLibPaths = TRUE
)

releaseLibrarySnapshot(snapshot = Sys.getenv("SPADES_PROJECT_LIB_SNAPSHOT"))

sweepLibrarySnapshots(where = getOption("spades.activeRunningPath"))
}
\arguments{
\item{packages}{Character vector of package specifications as accepted by
\code{Require::Install()}, e.g.
\code{"PredictiveEcology/reproducible@development (>= 3.2.1.9010)"}. Usually
the vector given to \code{setupProject(packages = )}, so the version floors
declared there are the single source of truth.}

\item{libPath}{The shared library. Defaults to \code{.libPaths()[1]}.}

\item{activeRunningPath}{Where the experiment keeps its \verb{Running_*} sentinels;
snapshots are created there as \verb{lib_<id>/} unless \code{where} says otherwise.}

\item{requireIdle}{Logical. Refuse to install while \code{libraryInUse()} finds a
worker. Set to \code{FALSE} only when you know the workers are idle.}

\item{installFun}{Function of \verb{(packages, libPath)} performing the install;
defaults to \code{Require::Install()}. Exposed for tests.}

\item{verbose}{Numeric.}

\item{where}{Directory holding snapshots; defaults to \code{activeRunningPath}.}

\item{id}{Snapshot id; defaults to \verb{<nodename>_<pid>} so
\code{sweepLibrarySnapshots()} can tell a live owner from a dead one.}

\item{setLibPaths}{Logical. Put the snapshot first on \code{.libPaths()} in place
of \code{libPath}, and record it in \code{SPADES_PROJECT_LIB_SNAPSHOT} so the worker
loop can release it at exit.}

\item{snapshot}{A directory returned by \code{snapshotLibrary()}.}
}
\value{
\code{syncProjectLibrary()} returns \code{TRUE} invisibly after installing,
\code{FALSE} when \code{packages} is empty. \code{libraryInUse()} returns a character
vector describing live workers (sentinel files and \code{ps} lines), empty when
none. \code{snapshotLibrary()} returns the snapshot directory.
\code{releaseLibrarySnapshot()} returns \code{TRUE} invisibly if it removed one.
\code{sweepLibrarySnapshots()} returns the directories it removed.
}
\description{
Every experiment backend (\code{experimentTmux()}, \code{experimentFuture()},
\code{experimentSBATCH()}) runs many jobs against one project library. Nothing
stops that library from being modified while jobs use it, and R lazy-loads
package objects from \verb{R/<pkg>.rdb} by file offset, so replacing a package
under a running session ends in
\verb{lazy-load database '.../R/<pkg>.rdb' is corrupt}. Two functions make the
library safe:
\itemize{
\item \code{syncProjectLibrary()} installs or updates packages \strong{once per launch}, in
a fresh R process (this session may itself have them loaded), and refuses
to run while any worker is alive. The backends call it when given
\verb{sync_library = <package specs>}.
\item \code{snapshotLibrary()} gives \strong{one job} its own copy of the library, made of
hardlinks (\code{file.link()}, so it costs seconds and no disk), falling back to
\code{file.copy()} across filesystems. A hardlink keeps the old inode alive: an
install into the shared library replaces files \emph{there} and cannot touch
what the running job has open. Every job thereby also records exactly which
package versions it ran with. The backends take the snapshot in the worker
startup script, before any package is loaded, when \code{snapshot_library = TRUE}
(the default).
}

\code{libraryInUse()} reports the workers that make a sync unsafe;
\code{releaseLibrarySnapshot()} removes one snapshot (the worker loop does this at
exit) and \code{sweepLibrarySnapshots()} removes those whose owning process is gone.
}
\examples{
\dontrun{
# before launching an experiment, with no worker running:
syncProjectLibrary(c("PredictiveEcology/reproducible@development (>= 3.2.1.9010)",
                     "PredictiveEcology/SpaDES.core@development"))
# inside a worker, before loading anything from the library:
snapshotLibrary(where = "logs/my_queue.rds")
}
}
