% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{as_scenario}
\alias{as_scenario}
\title{Coerce any scenario representation to a canonical record.}
\usage{
as_scenario(x, ...)
}
\arguments{
\item{x}{One of:
\itemize{
\item a \code{scenario} (returned as-is);
\item a character path or tarname (parsed via \code{\link[=pathParse]{pathParse()}}); vectorised;
\item a named list with the 5 fields (or aliases of them);
\item a single- or multi-row data.frame / tibble / dribble whose columns
map to the 5 fields, OR which has a \code{name}/\code{path}/\code{tarname} column
holding a path or tarname (auto-detected; force with \code{name_col}).
}}

\item{...}{Unused.}

\item{mapping}{Optional named character vector. Names are canonical
fields (\code{.ELFind}, ...); values are the actual column names in \code{x}.
Overrides the alias resolver.}

\item{name_col}{For data.frame input: column holding a path or tarname
to parse from. If \code{NULL}, field-mapping is tried first; if that fails
and a \code{name}/\code{path}/\code{tarname}/\code{file}/\code{filename} column exists, it is
used.}
}
\value{
A \code{scenario} (single input) or a list of \code{scenario}s
(vector / multi-row input).
}
\description{
Coerce any scenario representation to a canonical record.
}
