% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{reGet}
\alias{reGet}
\title{Download tarballs from Google Drive}
\usage{
reGet(gFiles, destDir, overwrite = FALSE, verbose = TRUE)
}
\arguments{
\item{gFiles}{Either a Google Drive \code{dribble} (e.g. the output of
\code{\link[=outList]{outList()}} / \code{\link[=outScenarios]{outScenarios()}}) or a character vector of Drive file IDs
or URLs.}

\item{destDir}{Character scalar. Local directory to write tarballs into.
Created if it does not exist.}

\item{overwrite}{Logical. Force re-download even if the local file
exists. Default \code{FALSE}.}

\item{verbose}{Logical. Print elapsed time per download. Default \code{TRUE}.}
}
\value{
A \code{data.table} with columns \code{name} and \code{local_path}, one row per
downloaded file.
}
\description{
Inverse of \code{\link[=outUpload]{outUpload()}}. Downloads one or more tar.gz archives from a
Google Drive folder to a local directory, using
\code{\link[reproducible:preProcess]{reproducible::preProcess()}} (so re-runs hit the local copy when present).
Vectorised: typically called with the multi-row \code{dribble} returned by
\code{\link[=outList]{outList()}} / \code{\link[=outScenarios]{outScenarios()}}.
}
\seealso{
\code{\link[=reUntar]{reUntar()}}, \code{\link[=reLoad]{reLoad()}}, \code{\link[=reGetUntarLoad]{reGetUntarLoad()}}, \code{\link[=outUpload]{outUpload()}}
}
