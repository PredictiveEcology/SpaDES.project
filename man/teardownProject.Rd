% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{teardownProject}
\alias{teardownProject}
\alias{.teardownProject}
\title{Tear down a project created by \code{setupProject()}}
\usage{
teardownProject(x, origLibPaths)

.teardownProject(prjPaths, origLibPaths)
}
\arguments{
\item{x}{Either the list returned by \code{\link[=setupProject]{setupProject()}}, or a character
vector of paths to remove (back-compat with the previous
\code{.teardownProject(prjPaths, origLibPaths)} signature).}

\item{origLibPaths}{Optional. The \code{.libPaths()} to restore. Defaults to
\code{x$paths$.previousLibPaths} when \code{x} is a \code{setupProject()} output, so
most callers will not need to supply this.}
}
\value{
\code{NULL}, invisibly. Called for its side effects.
}
\description{
Reverse the side-effects of \code{\link[=setupProject]{setupProject()}}:
}
\details{
\enumerate{
\item remove the project library directory created by \code{setupProject()},
\item unlink the project paths returned by \code{setupProject()},
\item restore the \code{.libPaths()} value that was in effect before
\code{setupProject()} was called.
}

The previous \code{.libPaths()} is stored on the \code{setupProject()} output as
\code{out$paths$.previousLibPaths} (and on \code{attr(out$paths, "extraPaths")}),
so \code{teardownProject(out)} is enough -- no need to remember
\code{origLibPaths} separately.
}
\seealso{
\code{\link[=setupProject]{setupProject()}} for what is being torn down.
}
