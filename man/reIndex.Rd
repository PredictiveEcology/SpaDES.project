% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remoteMember.R
\name{reIndex}
\alias{reIndex}
\alias{reGetMember}
\title{Read one member out of a remote tar archive, without downloading it}
\usage{
reIndex(gFile, destDir = tempdir())

reGetMember(gFile, member, index = NULL, file = NULL)
}
\arguments{
\item{gFile}{A Google Drive \code{dribble} row, file id, or URL for the \strong{tarball}.}

\item{destDir}{Directory for the downloaded index. Default \code{\link[=tempdir]{tempdir()}}.}

\item{member}{Character. The archive member to fetch. Matched exactly, or as a
regular expression when no exact match exists (it must then match one row).}

\item{index}{Optional \code{data.frame} from \code{\link[=reIndex]{reIndex()}}. Fetched if not supplied;
pass it when reading several members so the index is downloaded once.}

\item{file}{Optional path to write the raw member to. When \code{NULL} (default)
the member is read back with \code{\link[=readRDS]{readRDS()}} and returned.}
}
\value{
\code{reIndex()}: a \code{data.frame} with \code{name}, \code{offset} and \code{size}, one row
per archive member.

\code{reGetMember()}: the deserialized object, or the path written when
\code{file} is supplied.
}
\description{
\code{\link[=outTar]{outTar()}} writes an \emph{uncompressed} tar (R's \code{\link[utils:tar]{utils::tar()}} defaults to
\code{compression = "none"}), so every member sits at a known byte offset. Google
Drive answers HTTP \code{Range} requests with \verb{206 Partial Content}, so a single
object can be pulled out of a multi-GB archive by fetching only its bytes.

For a lazily saved \code{simList} this is the difference between downloading the
whole tarball and reading the one object you want: measured on a 4.63 GB
archive, one 6.2 MB object took \strong{0.83 s} against ~304 s for the whole file.

\code{\link[=reIndex]{reIndex()}} fetches the \verb{<runName>_index.rds} written beside the tarball;
\code{\link[=reGetMember]{reGetMember()}} uses it to fetch a single member.
}
\seealso{
\code{\link[=reGetMember]{reGetMember()}}, \code{\link[=outTar]{outTar()}}, \code{\link[=reGetUntarLoad]{reGetUntarLoad()}}
}
