% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdDiagnostics.R
\name{openFds}
\alias{openFds}
\title{Inspect open file descriptors.}
\usage{
openFds()
}
\value{
\code{data.frame} with columns \code{fd} (integer), \code{target} (character, with
any Linux \code{ (deleted)} suffix preserved), and \code{bucket} (character holder
category: \code{"socket"}, \code{"pipe"}, \code{"terra scratch"},
\code{"terra scratch (deleted)"}, \code{"tif (other)"}, \code{"vrt"}, \code{"sqlite"},
\code{"qs/qs2"}, \code{"anon_inode"}, \code{"other file"}, \code{"unknown"}).
}
\description{
On Linux, reads \verb{/proc/self/fd} and resolves each symlink to identify what
the fd points to. Returns a \code{data.frame} with one row per open fd; useful
for diagnosing the cryptic \code{"file descriptor is too large for select()"}
failure from \code{\link[parallelly:makeClusterPSOCK]{parallelly::makeClusterPSOCK()}}.
}
\details{
Returns an empty \code{data.frame} on non-Linux systems or if \verb{/proc/self/fd} is
unreadable.
}
\seealso{
\code{\link[=openFdsReport]{openFdsReport()}} for a printable summary; \code{\link[=fdSelectLimit]{fdSelectLimit()}}.
}
