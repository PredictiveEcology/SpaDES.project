% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment.R
\name{lastTraceback}
\alias{lastTraceback}
\title{Inspect the call stack from the most recent worker error}
\usage{
lastTraceback()
}
\value{
A list of calls (as from \code{sys.calls()}) suitable for passing to
\code{\link[base:traceback]{base::traceback()}}; \code{NULL} if no error has been captured in the current
session.
}
\description{
Worker panes started by \code{experimentTmux()} / \code{tmuxRunNextWorker()} evaluate
the user's \code{global.R} inside a fresh scenario environment (not \code{.GlobalEnv}).
When the source call errors, the package captures \code{sys.calls()} and stashes
it on that scenario env so a post-mortem traceback is still possible without
polluting the user's global state. Use this accessor to retrieve it.
}
\examples{
\dontrun{
  # After a worker pane errors:
  traceback(SpaDES.project::lastTraceback())
}
}
