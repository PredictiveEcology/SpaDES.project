% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdDiagnostics.R
\name{openFdsReport}
\alias{openFdsReport}
\title{Printable summary of open file descriptors.}
\usage{
openFdsReport(threshold = fdSelectLimit())
}
\arguments{
\item{threshold}{integer. Only fds at or above this number are bucketed.
Defaults to \code{\link[=fdSelectLimit]{fdSelectLimit()}}.}
}
\value{
character scalar; \code{""} if \verb{/proc/self/fd} is not available.
}
\description{
Wraps \code{\link[=openFds]{openFds()}} and returns a multi-line string suitable for warning or
error messages. By default summarizes only fds at or above
\code{\link[=fdSelectLimit]{fdSelectLimit()}} (the \code{select()} failure threshold); pass \code{threshold = 0L}
to summarize everything.
}
\examples{
cat(openFdsReport())
cat(openFdsReport(threshold = 0L))
}
