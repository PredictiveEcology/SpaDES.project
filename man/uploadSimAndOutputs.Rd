% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{uploadSimAndOutputs}
\alias{uploadSimAndOutputs}
\title{Save a SpaDES simulation and upload it with its outputs to Google Drive}
\usage{
uploadSimAndOutputs(runName, sim, gFolder = NULL, simFilename = NULL)
}
\arguments{
\item{runName}{Character scalar. Label used as the base name for both the
saved simulation file and the resulting tarball.}

\item{sim}{A \code{simList} object (the completed SpaDES simulation).}

\item{gFolder}{A Google Drive folder identifier accepted by
\code{\link[googledrive:drive_upload]{googledrive::drive_upload()}} — e.g. a \code{dribble}, a Drive URL, or a bare
folder ID created with \code{\link[googledrive:drive_id]{googledrive::as_id()}}.  Required; the function
stops if \code{NULL}.}

\item{simFilename}{Character scalar. Full path for the saved \code{.rds} file.
Defaults to the value returned by \code{\link[SpaDES.core:simFile]{SpaDES.core::simFile()}} using
\code{runName}, \code{SpaDES.core::outputPath(sim)}, \code{SpaDES.core::end(sim)}, and
extension \code{"rds"}.}
}
\value{
Invisibly returns the \code{\link[googledrive:drive_upload]{googledrive::drive_upload()}} result (a
\code{dribble} describing the uploaded file).
}
\description{
Saves a \code{simList} to an RDS file via \code{\link[SpaDES.core:saveSimList]{SpaDES.core::saveSimList()}}, bundles
it together with the simulation output files into a \code{.tar.gz} archive, and
uploads the archive to a Google Drive folder.
}
\seealso{
\code{\link[SpaDES.core:saveSimList]{SpaDES.core::saveSimList()}}, \code{\link[googledrive:drive_upload]{googledrive::drive_upload()}}
}
