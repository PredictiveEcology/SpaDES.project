% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{outTar}
\alias{outTar}
\title{Bundle a sim file and output files into a tar.gz archive}
\usage{
outTar(
  simFilename,
  outputFiles = character(0),
  runName,
  tarDir = dirname(simFilename),
  verbose = TRUE
)
}
\arguments{
\item{simFilename}{Character scalar. Path to the saved sim RDS file
(typically the return value of \code{\link[=outSave]{outSave()}}).}

\item{outputFiles}{Character vector of additional files to include (e.g.
\code{SpaDES.core::outputs(sim)$file}).  Non-existent paths are dropped.
Default \code{character(0)}.}

\item{runName}{Character scalar. Base name for the tarball
(\verb{<runName>.tar.gz}).}

\item{tarDir}{Character scalar. Directory in which to create the tarball.
Defaults to \code{dirname(simFilename)}.}

\item{verbose}{Logical. Pass \code{-v} to \code{tar} for file-by-file progress.
Default \code{TRUE}.}
}
\value{
Invisibly returns the path to the created tarball.
}
\description{
Creates a \code{.tar.gz} archive containing \code{simFilename} and any additional
\code{outputFiles}.  Files that do not exist are silently skipped so a partially
completed simulation can still be archived.
}
\seealso{
\code{\link[=outSave]{outSave()}}, \code{\link[=outUpload]{outUpload()}}, \code{\link[=outSaveTarUpload]{outSaveTarUpload()}}
}
