% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setupProject.R
\name{setup_family}
\alias{setup_family}
\alias{setup}
\title{Overview: the \verb{setup*} family}
\description{
\code{\link[=setupProject]{setupProject()}} is the high-level entry point that calls each of the
inner \verb{setup*} helpers in order. The helpers are normally invoked
indirectly via \code{setupProject()}, but each is exported so users can call
it on its own (for diagnosis, partial setup, or post-hoc tweaks).
}
\details{
Order in which \code{setupProject()} calls the helpers:
\enumerate{
\item \code{\link[=setupOptions]{setupOptions()}} (first pass -- before paths)
\item \code{\link[=setupPaths]{setupPaths()}}
\item \code{setupRestart} (internal)
\item \code{\link[=setupFunctions]{setupFunctions()}}
\item \code{\link[=setupModules]{setupModules()}}
\item \code{\link[=setupPackages]{setupPackages()}}
\item \code{\link[=setupSideEffects]{setupSideEffects()}}
\item \code{\link[=setupOptions]{setupOptions()}} (second pass -- after packages)
\item \code{\link[=setupParams]{setupParams()}}
\item \code{\link[=setupGitIgnore]{setupGitIgnore()}}
}

Other companions used directly by user code or by \code{setupProject()}:
\code{\link[=setupStudyArea]{setupStudyArea()}} (resolves a study-area polygon via \code{geodata::gadm()})
and \code{\link[=setupFiles]{setupFiles()}} (parses local / remote files into named lists).
}
\seealso{
\code{\link[=setupProject]{setupProject()}}, \code{\link[=setupPaths]{setupPaths()}}, \code{\link[=setupFunctions]{setupFunctions()}},
\code{\link[=setupSideEffects]{setupSideEffects()}}, \code{\link[=setupOptions]{setupOptions()}}, \code{\link[=setupModules]{setupModules()}},
\code{\link[=setupPackages]{setupPackages()}}, \code{\link[=setupParams]{setupParams()}}, \code{\link[=setupGitIgnore]{setupGitIgnore()}},
\code{\link[=setupStudyArea]{setupStudyArea()}}, \code{\link[=setupFiles]{setupFiles()}}.
}
\keyword{internal}
