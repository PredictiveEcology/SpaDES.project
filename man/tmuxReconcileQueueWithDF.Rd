% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux-queue.R
\name{tmuxReconcileQueueWithDF}
\alias{tmuxReconcileQueueWithDF}
\title{Reconcile an existing file-backed queue with a freshly built \code{df}}
\usage{
tmuxReconcileQueueWithDF(
  df,
  queue_path,
  onExistingQueue = c("resume", "append", "rebuild")
)
}
\arguments{
\item{df}{data.frame of runs, as passed to \code{\link[=experimentTmux]{experimentTmux()}}.}

\item{queue_path}{Character path to the queue \code{.rds}.}

\item{onExistingQueue}{What to do when \code{queue_path} already exists:
\itemize{
\item \code{"resume"} (default) -- keep the existing queue; warn if \code{df} holds rows it
does not, naming them and saying how to act on them;
\item \code{"append"} -- add those rows as \code{PENDING}, leaving existing rows and their
status untouched;
\item \code{"rebuild"} -- discard the existing queue and start again from \code{df}.
}}
}
\value{
Invisibly, \code{queue_path}.
}
\description{
\code{\link[=experimentTmux]{experimentTmux()}} materialises the queue from \code{df} only when \code{queue_path}
does not yet exist; an existing queue is authoritative, so a resumed run keeps
its \code{DONE}/\code{RUNNING} state instead of restarting finished work. That rule is
right, but it used to be applied \emph{silently}: a caller who had carefully rebuilt
\code{df} -- new scenarios added, finished ones dropped -- would watch the previous
queue run instead, with nothing to distinguish that from success. The only
way to act on the new \code{df} was to notice, and delete or rename the file.

This keeps the rule and makes it visible, and adds the middle option that was
missing: take the existing queue \emph{and} the rows \code{df} adds.

Rows are compared on the non-\code{meta_cols}, non-list columns -- the scenario
fields. List columns (\code{.modules}, \code{.times}) are payload, not identity.
}
