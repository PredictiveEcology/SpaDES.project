% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux.R
\name{experimentTmux}
\alias{experimentTmux}
\title{Run an experiment in tmux panes (single-shot or queue mode)}
\usage{
experimentTmux(
  df,
  global_path = "global.R",
  n_workers = 4,
  start_cmd = "R",
  delay_after_split = 0.2,
  delay_after_layout = 0.2,
  delay_between_R_start = 0,
  delay_before_source = 60,
  stagger_by = 0,
  set_mouse = TRUE,
  continue = FALSE,
  queue_path = NULL
)
}
\arguments{
\item{df}{data.frame; columns become objects in each worker pane.}

\item{global_path}{character; \strong{absolute} path to the script to \code{source()} in each pane.}

\item{n_workers}{integer; number of panes to spawn (default \code{4}).}

\item{start_cmd}{character; command to start R (default \code{"R"}).}

\item{delay_after_split}{numeric; tmux settle after splits (default \code{0.2}).}

\item{delay_after_layout}{numeric; tmux settle after layout (default \code{0.2}).}

\item{delay_between_R_start}{numeric; pause after starting R (default \code{0.0}).}

\item{delay_before_source}{numeric; panes 2..N initial sleep inside R (default \code{60.0}).}

\item{stagger_by}{numeric; extra offset per pane beyond pane 2 (default \code{0.0}).}

\item{set_mouse}{logical; enable tmux mouse via \verb{set-option -g mouse on} (default \code{TRUE}).}

\item{continue}{logical; \code{TRUE} for queue mode; \code{FALSE} for single-shot (default \code{FALSE}).}

\item{queue_path}{character; path for queue \code{.rds} when \code{continue=TRUE}; if \code{NULL}, defaults next to \code{global_path}.}
}
\value{
Invisibly returns the vector of worker pane IDs.
}
\description{
Spawns \code{n_workers} panes in the current tmux window, starts \strong{plain R},
and either:
\itemize{
\item \strong{single-shot}: assigns \strong{all columns} of \code{df} row \emph{i} to pane \emph{i}, then \code{source(global_path)} once; or
\item \strong{queue mode} (\code{continue=TRUE}): auto-writes a file-backed queue and each pane
loops: claim next \code{PENDING} row (atomic via file lock), assign \strong{all columns}, \code{source(global_path)},
mark \code{DONE}, repeat until no \code{PENDING}.
}

First pane: \strong{no initial delay}. Panes 2..N sleep inside R (\code{Sys.sleep}) before the first run.

Targeting uses tmux formats (\verb{#\{session_name\}:#\{window_index\}}) and pane IDs via
\verb{list-panes -F "#\{pane_id\}"}; commands are injected via \verb{send-keys … C-m}. See tmux manual.
}
