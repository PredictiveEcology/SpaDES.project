% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{reLoad}
\alias{reLoad}
\title{Load saved SpaDES simLists}
\usage{
reLoad(
  simFilenames,
  projectPath = getwd(),
  method = c("loadSimList", "readRDS"),
  ...
)
}
\arguments{
\item{simFilenames}{Character vector of paths to \code{.rds} files.}

\item{projectPath}{Character scalar. Passed to
\code{\link[SpaDES.core:loadSimList]{SpaDES.core::loadSimList()}} for relative-path resolution. Default
\code{getwd()}.}

\item{method}{One of \code{"loadSimList"} (default) or \code{"readRDS"}.}

\item{...}{Additional args forwarded to \code{\link[SpaDES.core:loadSimList]{SpaDES.core::loadSimList()}}
(ignored when \code{method = "readRDS"}).}
}
\value{
A list of \code{simList} objects, named by \code{basename(simFilenames)}.
}
\description{
Inverse of \code{\link[=outSave]{outSave()}}. Loads one or more \code{simList}s from \code{.rds} files
produced by \code{\link[=outSave]{outSave()}}. Defaults to \code{\link[SpaDES.core:loadSimList]{SpaDES.core::loadSimList()}};
set \code{method = "readRDS"} to bypass \code{.unwrap} entirely.

Note that \code{\link[SpaDES.core:saveSimList]{SpaDES.core::saveSimList()}} uses \code{.wrapResiliently} to NULL
out file-backed objects with inaccessible backing files at save time.
Load-time failures (e.g. backing files missing on this machine even
though they were present at save time) are independent of that, and are
handled by \code{loadSimList}'s pre-\code{.unwrap} resilient pass.
}
\seealso{
\code{\link[=reGet]{reGet()}}, \code{\link[=reUntar]{reUntar()}}, \code{\link[=reGetUntarLoad]{reGetUntarLoad()}}, \code{\link[=outSave]{outSave()}}
}
