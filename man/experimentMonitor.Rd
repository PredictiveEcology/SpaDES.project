% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux.R
\name{experimentMonitor}
\alias{experimentMonitor}
\title{Monitor live workers across an experiment (tmux panes or callr/cluster futures)}
\usage{
experimentMonitor(ef = NULL, queue_paths = NULL, stats = FALSE)
}
\arguments{
\item{ef}{Optional \code{"experimentFuture"} object (or list of them) whose
\code{queue_path} and \code{cores} will be used for discovery.  Switches
the function from tmux-scan mode to queue-scan mode.}

\item{queue_paths}{Optional character vector of queue \code{.rds} paths.
Equivalent to passing \code{ef = NULL} plus \code{queue_paths}; used when the
\code{ef} handle is no longer in scope (e.g. across R sessions).  When
\code{queue_paths} is supplied without \code{ef}, the SSH-alias probe is
skipped and \code{machine_name} from the queue is used verbatim as the
SSH target -- which only works if the OS hostname is itself a Host
entry in \verb{~/.ssh/config} / \verb{/etc/hosts}.}

\item{stats}{Logical.  When \code{TRUE}, queries \code{ps} per worker (locally
or via batched SSH) to append \code{state}, \code{cpuAvg} (percent CPU
averaged over the process's lifetime -- not the instantaneous rate
\code{htop} shows), \code{RAM (GB)} (resident memory), \code{availableCores}
(total CPUs on the node, from \code{nproc}), and \verb{total RAM (GB)}
(total RAM on the node, from \verb{/proc/meminfo}).  Default \code{FALSE}.}
}
\value{
Data.frame whose columns depend on the discovery mode:
\itemize{
\item \strong{tmux mode} -- \code{session}, \code{window}, \code{pane}, \code{pane_id},
\code{pane_ref} (the \code{"session:window.pane"} string), \code{title},
\code{node} (first dash-separated token in \code{title} that matches a
cluster alias from \verb{/etc/hosts}; falls back to
\code{\link[=localHostLabel]{localHostLabel()}} when the title contains only the raw local
hostname; \code{NA} if no match).
\item \strong{queue mode} -- \code{pid}, \code{machine}, \code{started_at}, \code{log_file}
(\code{NA} when the worker isn't a \code{callr::r_bg} writer), \code{queue_path},
\code{runName}.
}

With \code{stats = TRUE}, five additional columns appear in either
mode: \code{state}, \code{cpuAvg}, \code{RAM (GB)}, \code{availableCores},
\verb{total RAM (GB)}.  Returns an empty data.frame (0 rows, same
columns) if no workers are found.
}
\description{
Single read-only entry point for inspecting workers regardless of which
runner spawned them.  Discovery is driven by what you pass:
}
\details{
\itemize{
\item \strong{Default (\code{ef = NULL}, \code{queue_paths = NULL})} -- enumerates tmux
panes via \verb{tmux -S <socket> list-panes -a} across every tmux server
under \verb{$TMUX_TMPDIR/tmux-<uid>/}.  Same behaviour the historical
\code{tmuxListPanes()} had.  Per-socket failures are swallowed so one
broken socket cannot poison the rest; works outside a tmux pane
and across multiple tmux servers (e.g. sessions started under
different \code{-L} names).  \code{Cluster_Monitor} panes are filtered out.
\item \strong{\code{ef} supplied (or \code{queue_paths})} -- reads each queue file's
\code{status == "RUNNING"} rows, probes \verb{ssh <core> hostname -s} once
per non-local entry in \code{ef$cores} to map OS hostnames (which is
what \code{Sys.info()[["nodename"]]} writes to the queue) back to SSH
aliases (\verb{~/.ssh/config} / \verb{/etc/hosts} entries), and verifies
each PID is alive (\verb{/proc/<pid>} locally, batched
\verb{ssh <alias> "[ -d /proc/<pid> ]"} remotely).  This is the
\code{\link[=experimentFuture]{experimentFuture()}} / \code{\link[=experimentSBATCH]{experimentSBATCH()}} equivalent of the
tmux pane scan -- workers there don't necessarily live in a
tmux pane, so the queue file is the authoritative record.
}

Either way, \code{stats = TRUE} runs the same \verb{ps -o pid=,\%cpu=,rss=,state=}
batch (locally and via one SSH connection per remote node) to append
CPU / RSS / state plus per-node \code{nproc} / total RAM.
}
\section{State codes}{

The \code{state} column is the best single signal for hang-detection because
it is a snapshot (no time window needed).  Values:

\tabular{ll}{
\strong{State} \tab \strong{Meaning} \cr
\code{R} \tab running on CPU right now \cr
\code{S} \tab sleeping (waiting on I/O, timer, or lock) \cr
\code{D} \tab uninterruptible sleep (usually disk I/O; persistent \code{D} can indicate a hang) \cr
\code{T} \tab stopped (SIGSTOP or similar) \cr
\code{Z} \tab zombie (dead but not yet reaped) \cr
\code{Closed} \tab worker process has exited -- PID no longer exists \cr
\code{NA} \tab could not determine (machine unreachable, or no parseable \verb{<node>-<pid>} in title) \cr
}
}

\seealso{
\code{\link[=experimentFutureList]{experimentFutureList()}} for the same queue-mode discovery
plus cluster-wide kill / queue refresh / GS demotion.
\code{\link[=tmuxListPanes]{tmuxListPanes()}} is preserved as a thin alias that calls this
function with no \code{ef}.
}
