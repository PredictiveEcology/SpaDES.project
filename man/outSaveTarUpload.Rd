% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{outSaveTarUpload}
\alias{outSaveTarUpload}
\title{Save, tar, and upload a SpaDES simulation to Google Drive}
\usage{
outSaveTarUpload(
  runName,
  sim,
  gFolder = NULL,
  simFilename = NULL,
  tarDir = NULL,
  overwrite = TRUE,
  cleanup = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{runName}{Character scalar. Label used as the base name for the saved
sim file and tarball.}

\item{sim}{A \code{simList} object.}

\item{gFolder}{A Google Drive folder identifier; passed to \code{\link[=outUpload]{outUpload()}}.
Required.}

\item{simFilename}{Character scalar. Passed to \code{\link[=outSave]{outSave()}}; see that
function for the default.}

\item{tarDir}{Character scalar. Directory for the tarball; defaults to
\code{dirname(simFilename)}.  Passed to \code{\link[=outTar]{outTar()}}.}

\item{overwrite}{Logical. Passed to \code{\link[=outUpload]{outUpload()}}.  Default \code{TRUE}.}

\item{cleanup}{Logical. Delete the local tarball after upload.  Passed to
\code{\link[=outUpload]{outUpload()}}.  Default \code{FALSE}.}

\item{verbose}{Logical. Verbose tar output.  Passed to \code{\link[=outTar]{outTar()}}.
Default \code{TRUE}.}
}
\value{
Invisibly returns the \code{dribble} from \code{\link[googledrive:drive_upload]{googledrive::drive_upload()}}.
}
\description{
Convenience wrapper that calls \code{\link[=outSave]{outSave()}}, \code{\link[=outTar]{outTar()}}, and \code{\link[=outUpload]{outUpload()}}
in sequence.  The sim is saved to an RDS file, bundled with its output
files into a \code{.tar.gz} archive, and the archive is uploaded to a Google
Drive folder.
}
\seealso{
\code{\link[=outSave]{outSave()}}, \code{\link[=outTar]{outTar()}}, \code{\link[=outUpload]{outUpload()}}
}
