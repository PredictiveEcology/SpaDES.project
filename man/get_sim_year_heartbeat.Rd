% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmux.R
\name{get_sim_year_heartbeat}
\alias{get_sim_year_heartbeat}
\title{Heartbeat for year-checkpoint SpaDES simulations}
\usage{
get_sim_year_heartbeat(
  output_path,
  start_year = NULL,
  end_year = NULL,
  file_prefix = "cohortData"
)
}
\arguments{
\item{output_path}{Character. Directory to scan for checkpoint files.}

\item{start_year}{Integer or \code{NULL}.  Expected start year of the simulation.
If \code{NULL} (default), inferred as the minimum year found in \code{output_path}.}

\item{end_year}{Integer or \code{NULL}.  Expected end year.
If \code{NULL} (default), inferred as the maximum year found in \code{output_path}
(i.e. 100 \% is reported only once the final checkpoint exists).
Supply a value (e.g. \code{3020L}) to get a meaningful percentage before the
run completes.}

\item{file_prefix}{Character.  Only files whose basename begins with this
prefix are used as checkpoint indicators.  Defaults to \code{"cohortData"}
because that file is written \strong{after} all others at each SpaDES save
event, making it the most reliable completion signal.}
}
\value{
A named list with elements:
\describe{
\item{\code{ts}}{Character. Modification timestamp of the latest checkpoint file.}
\item{\code{iter}}{Integer. Simulation year of the latest checkpoint.}
\item{\code{started}}{Character. Modification timestamp of the first checkpoint file.}
\item{\code{elapsed}}{\code{difftime}. Wall-clock time between first and latest checkpoint.}
\item{\code{pct_complete}}{Numeric 0–100. Percentage of the simulation completed,
or \code{NA} if \code{start_year == end_year}.}
}
All elements are \code{NA} / \code{NA_character_} when no matching files are found.
}
\description{
Scans an output directory for files matching the pattern
\verb{<file_prefix>_year<XXXX>.<ext>} (e.g. \code{cohortData_year2920.rds}) and
returns the furthest simulation year reached, the wall-clock elapsed time
since the first checkpoint, and a percentage-complete estimate.
}
\examples{
\dontrun{
hb <- get_sim_year_heartbeat(
  output_path = "outputs/6.5/1991-2020/NRV_ssp370/rep1",
  end_year    = 3020L
)
message("Year: ", hb$iter, " (", hb$pct_complete, "\%) — last checkpoint: ", hb$ts)
}
}
